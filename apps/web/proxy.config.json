[
  {
    "context": "/api/v1/apps",
    "target": "http://localhost:8000/",
    "docs": "Placeholder — point at the Siaga Padi FastAPI backend once it exists (apps/backend). No backend is running yet; API calls will fail until then.",
    "logLevel": "debug",
    "secure": false,
    "changeOrigin": true,
    "pathRewrite": {
      "^/api/v1/apps": ""
    }
  }
]
